<!-- Skip Link -->
<nav>
    <a id="skip-to-content" href="#content">{% translate "skipLink", lang %}</a>
</nav>
<!-- END skip link -->

<header class="ignore-for-TOC header-footer-bg header-footer-color">
    {% uioTemplate %}

    {% localeLink lang, translationKey, category, collections %}

    <div class="wrapper">
        <a class="banner" href="/{% if not (config.defaultLanguage === lang) %}{{ config.languages[lang].slug }}{% endif %}" title="Home: Documentation Template">
            <span class="banner__logo">{% include 'svg/idrc-logo.svg' %}</span>
            <h1 class="banner__heading">{{ site[lang].title }}</h1>
        </a>

        <!-- Mobile Menu -->
        {# <nav class="">
            <ul id="menu">
                <li class=""><span class=""></span>
                    <ul class="">
                    {% include 'partials/menu.njk' %}
                   </ul>
                </li>
            </ul>
        </nav> #}

            <!-- "Desktop" Menu -->
        <nav class="menu" aria-label="menu">
            <ul class="inline-list">
            {% include 'partials/menu.njk' %}
            </ul>
        </nav>
    </div>
</header>
